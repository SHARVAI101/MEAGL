/*$(document).ready(function(){
	var current_pos=window.screen.top;		
	//$(window).bind('DOMMouseScroll', function(e){	   	
	$(".nextpara").on('click',function(e){  	
	    /*if(e.originalEvent.detail < 0) {
	        //scrolling up	        
	        
	        if(current_pos!=0)
	        {
	        	$("html,body").animate({scrollTop: current_pos-window.innerHeight},1000);
	        	current_pos-=window.innerHeight;
	        }
	    }
	    else{
	    	//scrolling down <closing comment here>

	        //if current window is not at the bottom of the document(window is the screen we see)
	        if(current_pos+window.innerHeight < $(document).height()) {
	        	$("html,body").animate({scrollTop: current_pos+window.innerHeight},1000);
		       	current_pos+=window.innerHeight;
		       	$(".nextpara").show();
		   	}
		   	
		   	if((current_pos+window.innerHeight+10)> $(document).height())
		   	{
		   		$(".nextpara").hide();
		   	}
	        
	    //}
	    
	});

	$(window).bind('DOMMouseScroll', function(e){	   	
	//$(".nextpara").on('click',function(e){  	
	    if(e.originalEvent.detail < 0) {
	        //scrolling up	        
	        
	        if(current_pos!=0)
	        {
	        	$("html,body").animate({scrollTop: current_pos-window.innerHeight},1000);
	        	current_pos-=window.innerHeight;
	        	$(".nextpara").show();
	        }
	    }
	    else{
	    	//scrolling down

	        //if current window is not at the bottom of the document(window is the screen we see)
	        /*if(current_pos+window.innerHeight < $(document).height()) {
	        	$("html,body").animate({scrollTop: current_pos+window.innerHeight},1000);
		       	current_pos+=window.innerHeight;
		   	} <closing comment here>
	        
	    }
	    
	});
	
});*/

